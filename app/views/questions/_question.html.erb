<div>
  <h3><%= question.body %></h3>
  <%= link_to 'Просмотр', question_path(question) %> |
  <%= link_to 'Редактировать', edit_question_path(question) %> |
  <%= link_to 'Удалить', question_path(question), method: :delete, data: { confirm: 'Вы уверены?' } %>
</div>
