<div class="container-fluid bg-primary text-light py-4">
  <div class="container">
    <div class="row justify-content-between align-items-center">
      <div class="col-md-3">
        <p class="mb-0 h3"><%= link_to "TestGuru Admin Panel", root_path, class: "text-light", style: "text-decoration: none;" %></p>
      </div>
      <div class="col-md-9">
        <nav class="navbar navbar-expand-lg navbar-light">
          <% if user_signed_in? %>
            <span class="navbar-text me-3">Welcome, <b><%= current_user.email %></b> Guru</span>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item"><%= link_to t('header.logout'), destroy_user_session_path, method: :delete, class: "nav-link text-light" %></li>
              <li class="nav-item"><%= link_to 'Gists', admin_gists_path, class: "nav-link text-light"  %></li>
            </ul>
          <% end %>
        </nav>
      </div>
    </div>
  </div>
</div>
