<h1><%= t('.header') %></h1>
<%= link_to t('links.add'), new_admin_badge_path, class: "btn btn-primary" %>
<%= render 'shared/errors', resource: @test %>

<div class="container mt-4">
  <table class="table">
    <thead>
      <tr>
        <th style="text-align: center;"><%= t('.name') %></th>
        <th style="text-align: center;"><%= t('.image') %></th>
        <th colspan="3" style="text-align: center;"><%= t('.actions') %></th>
      </tr>
    </thead>

    <tbody>
      <%= render @badges %>
    </tbody>
  </table>
</div>

<%= link_to t('links.back'), admin_tests_path, class: 'btn btn-secondary' %>
