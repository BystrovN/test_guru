<div>
  <h3><%= answer.body %></h3>
  <p><%= t('.correct', correct: answer.correct) %></p>
  <%= link_to t('links.show'), admin_answer_path(answer) %> |
  <%= link_to t('links.edit'), edit_admin_answer_path(answer) %> |
  <%= link_to t('links.delete'), admin_answer_path(answer), method: :delete, data: { confirm: t('actions.sure?') } %>
</div>
